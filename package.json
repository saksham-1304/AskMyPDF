{
  "name": "pdf-chat-app",
  "version": "1.0.0",
  "description": "A full-stack MERN application for chatting with PDF documents using AI",
  "main": "backend/server.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build:full --prefix backend",
    "start": "npm run start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "clean": "rm -rf backend/public && rm -rf frontend/dist && rm -rf backend/node_modules && rm -rf frontend/node_modules && rm -rf node_modules"
  },
  "keywords": [
    "pdf",
    "chat",
    "ai",
    "mern",
    "react",
    "nodejs",
    "mongodb",
    "gemini",
    "pinecone"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "your-repository-url"
  }
}